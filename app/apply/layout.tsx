import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply",
  description:
    "Apply for construction roles or enquire about hiring talent with Neura Recruitment. Choose your path to get started.",
};

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
